"""EBS Config page."""

import streamlit as st

from styles import render_title

render_title(
    "EBS Config",
    subtitle="Configure EBS clone parameters and options.",
)

st.write("EBS Config content goes here.")
