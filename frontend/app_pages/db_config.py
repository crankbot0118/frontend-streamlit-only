"""DB Config page."""

import streamlit as st

from styles import render_title

render_title(
    "DB Config",
    subtitle="Set up database connection and clone settings.",
)

st.write("DB Config content goes here.")
